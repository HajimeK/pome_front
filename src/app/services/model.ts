export type Tag = {
    id: number, // -1 if not assigned in DB
    tag: string
};

export type Experience = {
    id: number, // -1 if not assigned in DB
    title: string,
    note: string,
    urle: string
};
